__pycache__/
*.pyc
.venv/
.env
*.log
.DS_Store
pgdata/
data/local/
*.tgz
eval/external/

# Node / build output
node_modules/
air-server/dist/
dist/

# Agent runtime state (build provenance lives in docs/report.md)
.gjc/
.pytest_cache/

# Keep evidence logs even though *.log is ignored
!eval/results/replica-spike.log

# Sponsor dataset — licence says contest use only, so it is fetched, never vendored.
# Provenance + integrity: datasets/MANIFEST.md, fetch with scripts/fetch-companyx-dataset.sh
datasets/companyx-dataset-v1.0.zip
datasets/companyx-v1.0/
.eval*.log

# npm pack 직전에 저장소 루트에서 복사되는 사본 (air-server/scripts/prepack.mjs)
air-server/README.md
air-server/LICENSE
air-server/NOTICE
air-server/*.tgz
