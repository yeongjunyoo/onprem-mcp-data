# 보안 정책 (Security Policy)

## 지원 범위

이 저장소는 사내 데이터베이스에 접근하는 MCP 서버입니다. 보안 표면이 세 곳에 있습니다.

1. **SQL 실행 경로** — 도구가 실행하는 모든 쿼리는 읽기 전용 트랜잭션에서, 최소권한 롤(`mcp_ro`)로 강등된 상태로 돕니다. 쓰기 구문, 다중 구문 연결, 수퍼유저 함수는 거부되고 statement/lock timeout이 걸립니다.
2. **컨텍스트 접지** — 모델이 답변에 쓰는 근거는 큐레이션된 컨텍스트로 제한됩니다. 질의가 지목한 개체를 온톨로지에서 해소하지 못하면 컨텍스트를 비우고 모른다고 답하도록 되어 있습니다.
3. **로컬 실행** — 생성·임베딩 모델은 로컬 Ollama에서만 돕니다. 데이터가 외부로 나가는 경로는 설계상 없습니다.

이 셋 중 하나가 우회되는 것은 보안 문제로 취급합니다. 예: 읽기 전용 가드를 통과하는 쓰기 구문, 권한이 없어야 할 테이블의 노출, 컨텍스트 밖 데이터가 답변에 유입되는 경로, 로컬 외부로 나가는 네트워크 호출.

## 신고 방법

**공개 이슈로 올리지 마세요.** GitHub의 비공개 취약점 신고(Security 탭 > Report a vulnerability)를 이용하거나, 저장소 소유자에게 직접 연락해 주세요.

신고에 아래를 담아 주시면 확인이 빨라집니다.

- 영향받는 구성요소(도구 이름, 파일 경로)
- 재현 절차와 최소 재현 입력
- 관측된 결과와 기대 결과
- 실행 환경(PostgreSQL·pgvector·Ollama 버전, 모델 태그)

## 대응

이 프로젝트는 1인이 유지하며, 신고 확인은 영업일 기준 3일 이내를 목표로 합니다. 수정 후에는 재현 테스트를 추가하고 그 결과를 커밋합니다. 보고자가 원하지 않으면 이름을 공개하지 않습니다.

## 범위 밖

- 제3자 의존성 자체의 알려진 취약점은 업스트림에 신고해 주세요. 다만 이 저장소가 그 취약한 버전을 고정하고 있다면 알려 주시면 갱신합니다. 의존성 목록은 `docs/sbom.md`에 있습니다.
- 로컬에 이미 관리자 권한이 있는 사용자가 자신의 데이터베이스를 조작하는 시나리오는 위협 모델 밖입니다.
